# Skills — save only after a clean run

Official order: do the job once → save the skill → then maybe a routine.

A useful skill states when to use it, inputs and access, sequence, validation, what to return, and what requires approval.

Night 0 candidates: Watch open/close (Shift Lead), Bench gap (Bench), Pre-stream note (Signal).

Paste after a good run:

```
Save the process we just used as a skill called "{name}".
Include sources, output path under /workspace/countershift/, validation, and the rule that spend, mail, X posts, production deploy, and talking to SpaceXAI staff always require Shawn.
```
